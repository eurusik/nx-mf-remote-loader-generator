import remoteComponentGenerator from './generators/remote-component/remote-component';

export { remoteComponentGenerator };

export const generators = {
  remoteComponent: remoteComponentGenerator
};

export default generators;
